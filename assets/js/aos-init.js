// AOS initialization
document.addEventListener("DOMContentLoaded", function () {
  AOS.init({
    // Global settings
    duration: 800,
    easing: "ease-in-out",
    once: true,
    offset: 100,
  });
});
